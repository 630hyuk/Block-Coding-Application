type UserData @model @auth(rules: [{allow: public}]) {
  id: ID!
  email: AWSEmail!
  nickname: String!
  progress: String
  registerStamp: AWSDateTime!
  lastLoginStamp: AWSDateTime
  phone: AWSPhone
}
 